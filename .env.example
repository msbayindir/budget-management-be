# Environment Configuration
NODE_ENV=development
PORT=3000

# Database Configuration
DATABASE_URL="mongodb://localhost:27017/budget_management"

# JWT Configuration - CRITICAL: Use strong, unique secrets in production
# Generate strong secrets using: openssl rand -base64 32
JWT_ACCESS_SECRET="your-super-secret-access-key-here-minimum-32-characters"
JWT_REFRESH_SECRET="your-different-super-secret-refresh-key-here-minimum-32-characters"
JWT_ACCESS_EXPIRES_IN="15m"
JWT_REFRESH_EXPIRES_IN="7d"

# CORS Configuration
CORS_ORIGIN="http://localhost:3000"

# Cookie Configuration (Production only)
COOKIE_DOMAIN="yourdomain.com"

# Security Notes:
# 1. Never use default secrets in production
# 2. Use different secrets for access and refresh tokens
# 3. Keep secrets secure and rotate them regularly
# 4. Set COOKIE_DOMAIN for production deployments
